<odoo>
  <data>
    <record
      id="active_community_members_formula"
      model="contract.line.qty.formula"
    >
      <field name="name">Effective cooperators range</field>
      <field name="code">
company = env['res.company'].sudo().search([('id','=',contract.community_company_id.id)])
result = company.sudo().get_numberof_effective_cooperators_range()
      </field>
      <field name="company_id" eval="ref('base.main_company')" />
    </record>
  </data>
</odoo>
