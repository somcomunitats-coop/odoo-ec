<odoo>
  <record id="share_line_form" model="ir.ui.view">
      <field name="name">share.line.form</field>
      <field name="model">share.line</field>
      <field name="arch" type="xml">
          <form>
            <group>
              <field name="partner_id" readonly="0" required="1" invisible="1" />
              <field name="company_id" readonly="0" required="1" invisible="1" />
              <field name="effective_date" readonly="0" required="1" />
              <field name="share_product_id" domain="[('is_share','=',True)]" />
              <field name="share_number" readonly="0" />
              <field name="share_unit_price" />
              <field name="total_amount_line" />
            </group>
            <footer>
              <field name="id" invisible="1" />
              <button
            string="Create"
            name="action_close"
            type="object"
            class="btn-primary"
            attrs="{'invisible': [('id', '!=', False)]}"
          />
              <button string="Cancel" class="btn-secondary" special="cancel" />
            </footer>
          </form>
      </field>
  </record>
</odoo>
